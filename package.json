{
  "name": "costume-rental-marketplace",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend", "mobile", "shared"],
  "scripts": {
    "prepare": "husky install",
    "lint": "npm run lint -w frontend && npm run lint -w backend && npm run lint -w mobile",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend && npm run build -w mobile",
    "dev": "concurrently -k -n backend,frontend 'npm run dev -w backend' 'npm run dev -w frontend'",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.6.0",
    "@typescript-eslint/parser": "^7.6.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "husky": "^9.0.0",
    "prettier": "^3.2.5"
  }
}
